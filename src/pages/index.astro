---
import { getCollection } from "astro:content";
import PageLayout from "@/layouts/Base";
import PostPreview from "@/components/blog/PostPreview";
import SocialList from "@/components/SocialList";
import { sortMDByDate } from "@/utils";

const MAX_POSTS = 10;
const allPosts = await getCollection("post");
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);
---

<PageLayout meta={{ title: "Home" }}>
	<section>
		<h1 class="title mb-6">Hello there!</h1>
		<p class="mb-4">
			Hi, I'm Daniele Atzeni and I work as a Software Developer in Rapallo, Italy. <br />
			Here you can find what I am currently reading and what are the books I just finished, I impose
			myself to keep this updated so I have a place, somewhere that helps me keep track of this list.<br
			/>
			Oh, you can also see what my dev-skills are, you can follow any of link below to reach me along
			with the Résumé on the top.
		</p>
		<SocialList />
	</section>
	<!-- <section aria-label="Blog post list" class="mt-16">
		<h2 class="title mb-4 text-xl">Posts</h2>
		<ul class="space-y-4 sm:space-y-2">
			{
				allPostsByDate.map((p) => (
					<li class="flex flex-col gap-x-2 sm:flex-row">
						<PostPreview post={p} />
					</li>
				))
			}
		</ul>
	</section> -->
	<section class="mt-16">
		<h2 class="title mb-4 text-xl">Technologies</h2>
		<ul class="space-y-4 sm:space-y-2">
			<li>C, C++, C#, Java, Groovy, PL/pgSQL, OCaml, PHP, JavaScript, Typescript, Kotlin</li>
			<li>Angular, HTML, CSS, JSP</li>
			<li>PostgreSQL, MySQL, Oracle SQL</li>
			<li>Git, Ninject, Entity Framework, XUnit, LoRaWAN</li>
			<li>Spring Boot, Ktor</li>
			<li>Arduino, MQTT, LaTeX, HL7, Mirth</li>
			<li>Linux, MacOS, Windows</li>
		</ul>
	</section>
</PageLayout>
